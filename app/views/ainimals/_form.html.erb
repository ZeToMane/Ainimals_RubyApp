<%= form_with(model: ainimal) do |form| %>
  <% if ainimal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ainimal.errors.count, "error") %> prohibited this ainimal from being saved:</h2>

      <ul>
        <% ainimal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :class, style: "display: block" %>
    <%= form.text_field :class %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.text_area :image %>
  </div>

  <div>
    <%= form.label :desc, style: "display: block" %>
    <%= form.text_area :desc %>
  </div>

  <div>
    <%= form.submit class: 'btn btn-primary'%>
  </div>
<% end %>
