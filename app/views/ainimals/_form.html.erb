<%= form_with(model: ainimal) do |form| %>
  <% if ainimal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ainimal.errors.count, "error") %> prohibited this ainimal from being saved:</h2>

      <ul>
        <% ainimal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= simple_form_for(ainimal) do |f| %>
    <div class="container col-5">
      <%= f.label :name, style: "display: block" %>
      <%= f.text_field :name, class: 'form-control' %>
    </div>

    <div class="container col-5">
      <%= f.label :animaltype, style: "display: block" %>
      <%= f.text_field :animaltype, class: 'form-control' %>
    </div>

    <div class="container col-5">
      <%= f.label :image, style: "display: block" %>
      <%= f.text_area :image, class: 'form-control' %>
    </div>

    <div class="container col-5">
      <%= f.label :desc, style: "display: block" %>
      <%= f.text_area :desc, class: 'form-control' %>
    </div>

    <div class="container col-5 mt-3 text-center">
      <%= f.submit class: 'btn btn-dark'%>
    </div>
  <% end %>
<% end %>
